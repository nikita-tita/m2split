# 🚀 Инструкция по деплою на GitHub Pages

## 📦 Коммиты, ожидающие деплоя

**Ветка:** `claude/fix-developers-projects-loading-011xpEMXqFWDkJJz8tfeLkD9`

### Список изменений (3 коммита):

```
e2c4180 - docs: Add comprehensive system review document
8b1db6f - fix: Improve text contrast for better accessibility (WCAG AA)
416b889 - fix: Add universal tariff and fix developer IDs for commission calculation
```

### Что включено:

#### ✅ Исправления контрастности (WCAG AA)
- Все текстовые поля теперь имеют черный цвет (text-gray-900)
- Input labels, helper text, table headers - всё читается четко
- Соответствие стандарту WCAG AA (контраст 4.5:1)

#### ✅ Исправление тарифов
- Добавлен универсальный тариф (2.5%) для всех застройщиков
- Исправлен ID застройщика Самолет (dev-samolet → dev-samolot)
- Тариф Level Group (3.0%)
- Комиссия теперь считается для ВСЕХ застройщиков

#### ✅ Документация
- Полный системный обзор (SYSTEM_REVIEW.md)
- 481 строка документации с архитектурой, ролями, БД, рекомендациями

---

## 🔧 Текущая конфигурация

### Next.js настройки (next.config.js)
```javascript
output: 'export',                              // Static Site Generation
basePath: '/m2split',                          // Base path для GitHub Pages
images: { unoptimized: true },                 // Без оптимизации изображений
```

### GitHub Actions (.github/workflows/deploy.yml)
- **Триггер**: Push в ветку `main`
- **Билд**: `npm run build` → генерация в `/out`
- **Деплой**: Автоматически на ветку `gh-pages`

### URL продакшена
```
https://nikita-tita.github.io/m2split/
```

---

## 📋 Пошаговая инструкция деплоя

### Вариант 1: Через Pull Request (Рекомендуется)

#### Шаг 1: Создать Pull Request

```bash
# Убедитесь что все изменения запушены
git push origin claude/fix-developers-projects-loading-011xpEMXqFWDkJJz8tfeLkD9

# Создайте PR на GitHub:
https://github.com/nikita-tita/m2split/pull/new/claude/fix-developers-projects-loading-011xpEMXqFWDkJJz8tfeLkD9
```

#### Шаг 2: Review & Merge
1. Откройте PR на GitHub
2. Проверьте изменения (3 коммита)
3. Нажмите **"Merge pull request"** → **"Confirm merge"**
4. Дождитесь завершения GitHub Actions (1-2 минуты)

#### Шаг 3: Проверка деплоя
1. Откройте https://github.com/nikita-tita/m2split/actions
2. Дождитесь ✅ успешного выполнения workflow "Deploy to GitHub Pages"
3. Проверьте сайт: https://nikita-tita.github.io/m2split/

---

### Вариант 2: Прямой merge в main (Быстрый способ)

```bash
# 1. Переключиться на main и получить последние изменения
git checkout main
git pull origin main

# 2. Смержить вашу ветку
git merge claude/fix-developers-projects-loading-011xpEMXqFWDkJJz8tfeLkD9

# 3. Запушить в main (автоматически запустит деплой)
git push origin main
```

#### ⚠️ После push в main:
- GitHub Actions автоматически запустит билд
- Результат будет в ветке `gh-pages`
- Сайт обновится через 1-2 минуты

---

### Вариант 3: Локальный тест перед деплоем

```bash
# 1. Собрать проект локально
npm run build

# 2. Проверить что создалась папка /out
ls -la out/

# 3. Локальный просмотр (установить serve если нет)
npm install -g serve
serve out -p 3000

# 4. Открыть http://localhost:3000/m2split/
```

Если всё работает → продолжить с Вариантом 1 или 2.

---

## 🔍 Проверка после деплоя

### 1. Проверка GitHub Actions
```
https://github.com/nikita-tita/m2split/actions
```
- Статус должен быть ✅ зеленый
- Последний workflow: "Deploy to GitHub Pages"
- Время выполнения: ~1-2 минуты

### 2. Проверка контента
Откройте https://nikita-tita.github.io/m2split/ и проверьте:

#### ✅ Контрастность текста
- [ ] Input labels - **черный текст** (не серый)
- [ ] Select options - **черный текст**
- [ ] Table headers - **черный текст, жирный**
- [ ] Helper text - **темно-серый** (но читаемый)

#### ✅ Застройщики
- [ ] В форме создания сделки (`/deals/new`)
- [ ] Выпадающий список "Застройщик" показывает:
  - Группа «Самолет»
  - Level Group
  - Брусника
  - MR Group
  - И другие (всего 16)
- [ ] НЕ показывает Петрова/Сидорова

#### ✅ Проекты и квартиры
- [ ] После выбора застройщика загружаются проекты
- [ ] После выбора проекта загружаются 7 квартир
- [ ] После выбора квартиры автозаполняются:
  - Название объекта
  - Адрес
  - Лот
  - Цена

#### ✅ Расчет комиссии
- [ ] После выбора квартиры показывается блок "Расчет комиссии КВН"
- [ ] Тариф отображается (2.5%, 2.7% или 3.0%)
- [ ] Комиссия рассчитывается автоматически

#### ✅ Документация
- [ ] Файл `SYSTEM_REVIEW.md` доступен в репозитории
- [ ] Содержит 481 строку с полной документацией

---

## 🐛 Troubleshooting (Решение проблем)

### Проблема 1: GitHub Actions не запустился
**Причина**: Возможно нужны права на запуск Actions

**Решение**:
1. Settings → Actions → General
2. Workflow permissions → "Read and write permissions"
3. Save

### Проблема 2: 404 на GitHub Pages
**Причина**: GitHub Pages не включен

**Решение**:
1. Settings → Pages
2. Source: Deploy from a branch
3. Branch: `gh-pages` / `root`
4. Save

### Проблема 3: Билд падает с ошибкой
**Причина**: Обычно TypeScript ошибки или missing dependencies

**Решение**:
```bash
# Проверить локально
npm ci                    # Чистая установка зависимостей
npm run build             # Попробовать собрать
npm run lint              # Проверить линтер
```

### Проблема 4: Старая версия на сайте
**Причина**: Кеш браузера

**Решение**:
1. Очистить кеш: Ctrl+Shift+R (Chrome/Edge)
2. Hard reload: Ctrl+F5
3. Или открыть в режиме инкогнито

### Проблема 5: basePath проблемы (404 на ресурсах)
**Причина**: Неправильный basePath в конфиге

**Проверка**: В `next.config.js` должно быть:
```javascript
basePath: process.env.NODE_ENV === 'production' ? '/m2split' : ''
```

---

## 📊 История деплоев

### 2025-11-13 - Текущие изменения (готовы к деплою)
- ✅ Контрастность текста (WCAG AA)
- ✅ Универсальный тариф (2.5%)
- ✅ Исправлены ID застройщиков
- ✅ Добавлена полная документация (SYSTEM_REVIEW.md)

### Предыдущие деплои (уже на проде)
```
65c8c8e - feat: Add role-specific onboarding tips to all pages
149bf58 - fix: Change helpText to helperText in Input components
dc2c9ef - feat: Add apartment selection flow with auto-fill
a299b9e - feat: Add apartment selection flow for deal creation
a58a1ad - fix: Enable CONTRACTOR role to create deals and add onboarding
```

---

## 📝 Чеклист перед деплоем

- [ ] Все изменения закоммичены
- [ ] Все изменения запушены в feature branch
- [ ] Локальный билд успешен (`npm run build`)
- [ ] Нет TypeScript ошибок (`npm run lint`)
- [ ] Протестировано локально в `/out` папке
- [ ] Создан Pull Request (или готов merge в main)
- [ ] Проверен список изменений (3 коммита)
- [ ] Readme/документация обновлена (если нужно)

---

## 🚀 Quick Deploy (Для срочного деплоя)

```bash
# Один лайнер для деплоя
git checkout main && \
git pull origin main && \
git merge claude/fix-developers-projects-loading-011xpEMXqFWDkJJz8tfeLkD9 && \
git push origin main

# Затем открыть в браузере через 2 минуты:
# https://nikita-tita.github.io/m2split/
```

---

## 📞 Поддержка

**Вопросы по деплою?**
- Проверьте GitHub Actions: https://github.com/nikita-tita/m2split/actions
- Проверьте GitHub Pages: https://github.com/nikita-tita/m2split/settings/pages
- Проверьте SYSTEM_REVIEW.md для архитектурных вопросов

**Автор документации:** Claude (AI Assistant)
**Дата:** 2025-11-13
**Версия:** 1.0
