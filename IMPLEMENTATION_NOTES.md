# Заметки по имплементации расширенного ТЗ

## Выполнено

### 1. Типы данных обновлены ✅

**Файл:** `types/index.ts`

**Добавлено:**
- ✅ Расширенные роли: `AGENCY_ADMIN`, `ACCOUNTANT`
- ✅ Интерфейс `RoleEmulation` для эмуляции ролей
- ✅ Поле `canEmulateRoles` в `User`
- ✅ Интерфейс `DealInitiator` (immutable)
- ✅ Поле `initiator` в `Deal`
- ✅ Расширенный `AuditLog` с полями эмуляции
- ✅ Обновлены статусы сделок: добавлен `APPROVED`, `SENT_TO_BANK`

### 2. Документация создана ✅

**Файл:** `EXTENDED_REQUIREMENTS.md`

**Содержит:**
- ✅ Полное описание ролей и матрицы прав
- ✅ Механизм эмуляции ролей
- ✅ Модель данных с полной видимостью полей
- ✅ ACL по стадиям (Stage Access Control)
- ✅ Требования к UI и валидациям
- ✅ Структура audit log
- ✅ Критерии приёмки
- ✅ Примеры UI элементов

## Требуется для полной имплементации

### 3. Компоненты UI (в разработке)

#### 3.1 RoleEmulationBanner
**Путь:** `components/RoleEmulationBanner.tsx`
**Функция:** Баннер предупреждения при эмуляции роли
**Состояние:** Требуется создание

```typescript
interface Props {
  actedAsRole: UserRole;
  actedAsPartyName: string;
  realUserName: string;
  realRole: UserRole;
  onExit: () => void;
}
```

#### 3.2 RoleSelector
**Путь:** `components/RoleSelector.tsx`
**Функция:** Селектор для переключения роли (в шапке профиля)
**Состояние:** Требуется создание

```typescript
interface Props {
  currentRole: UserRole;
  canEmulate: boolean;
  availableRoles: UserRole[];
  onRoleChange: (role: UserRole, partyId?: string) => void;
}
```

#### 3.3 DealInitiatorBlock
**Путь:** `components/deals/DealInitiatorBlock.tsx`
**Функция:** Блок отображения инициатора сделки
**Состояние:** Требуется создание

```typescript
interface Props {
  initiator: DealInitiator;
  partyName: string;
  userName: string;
}
```

#### 3.4 FieldAccessInfo
**Путь:** `components/ui/FieldAccessInfo.tsx`
**Функция:** Tooltip с информацией о доступе к полю
**Состояние:** Требуется создание

```typescript
interface Props {
  fieldName: string;
  dbSource: string; // e.g., "DEAL.shares[].amount"
  currentRole: UserRole;
  accessReason: string; // e.g., "R-only после APPROVED"
}
```

### 4. Обновление Store

**Файл:** `lib/store.ts`

**Требуется добавить:**
```typescript
interface AppState {
  // ... existing

  // Role emulation
  roleEmulation: RoleEmulation | null;
  setRoleEmulation: (emulation: RoleEmulation | null) => void;
  exitEmulation: () => void;

  // Audit tracking
  createAuditLog: (entry: Omit<AuditLog, 'id' | 'createdAt'>) => void;
}
```

### 5. Обновление Mock Data

**Файл:** `lib/mock-data.ts`

**Требуется обновить:**
- ✅ Добавить `partyId` и `canEmulateRoles` к users
- ✅ Добавить `initiator` к deals
- ✅ Создать mock для `RoleEmulation`
- ✅ Создать примеры `AuditLog` с эмуляцией

### 6. Обновление компонентов

#### 6.1 Layout.tsx
**Добавить:**
- Селектор роли в шапке (при `canEmulateRoles`)
- Баннер эмуляции (при `isEmulating`)
- Обновить labels ролей (добавить новые)

#### 6.2 Dashboard page
**Изменить:**
- Фильтрация данных по эмулируемой роли
- Скрыть недоступные метрики для роли

#### 6.3 Deals pages
**Добавить:**
- Блок инициатора в шапке карточки
- Disabled fields с FieldAccessInfo
- Валидация stage-based ACL

### 7. Утилиты доступа

**Файл:** `lib/access-control.ts` (новый)

**Функции:**
```typescript
// Проверка доступа к полю
canEditField(
  field: string,
  entityType: string,
  stage: DealStatus | RegistryStatus,
  userRole: UserRole
): boolean

// Проверка доступа к сущности
canAccessEntity(
  entityId: string,
  entityType: string,
  userRole: UserRole,
  partyId: string
): boolean

// Получение причины недоступности
getAccessReason(
  field: string,
  stage: string,
  role: UserRole
): string

// Фильтрация данных по роли
filterByRoleAccess<T>(
  data: T[],
  userRole: UserRole,
  partyId: string
): T[]
```

### 8. Валидации (расширенные)

**Файл:** `lib/validations.ts`

**Добавить:**
```typescript
// Валидация stage transitions
canTransitionStage(
  currentStage: DealStatus,
  targetStage: DealStatus,
  userRole: UserRole
): boolean

// Валидация суммы долей = 100%
validateSharesTotal(shares: DealShare[]): ValidationResult

// Валидация идемпотентности
validateRegistryIdempotency(
  registryId: string,
  existingRegistries: Registry[]
): boolean
```

## Примерный план внедрения

### Фаза 1: Типы и Store (1-2 часа)
- [x] Обновить типы
- [ ] Расширить store с эмуляцией
- [ ] Обновить mock data

### Фаза 2: Компоненты доступа (2-3 часа)
- [ ] RoleEmulationBanner
- [ ] RoleSelector
- [ ] DealInitiatorBlock
- [ ] FieldAccessInfo
- [ ] Access control utilities

### Фаза 3: Интеграция в страницы (3-4 часа)
- [ ] Layout с эмуляцией
- [ ] Dashboard с фильтрацией
- [ ] Deals с initiator и ACL
- [ ] Registries с stage validation

### Фаза 4: Тестирование (2-3 часа)
- [ ] Сценарии эмуляции
- [ ] Stage transitions
- [ ] Field access control
- [ ] Audit log tracking

**Общее время:** ~8-12 часов разработки

## Критические моменты

### ⚠️ Важно для деплоя

1. **Идемпотентность:** REGISTRY.id должен быть уникальным и проверяться перед отправкой
2. **Audit log:** Все изменения должны логироваться с полным контекстом
3. **Stage ACL:** Переходы между статусами должны валидироваться на backend
4. **Emulation scope:** Эмуляция НЕ расширяет доступ к данным

### 🔒 Безопасность

1. API должен валидировать заголовки `X-Act-As-Role` и `X-Act-As-Party`
2. Backend должен проверять `can_emulate_roles` флаг
3. Все действия в эмуляции должны логироваться отдельно
4. Frontend не должен полагаться только на client-side ACL

## Текущее состояние проекта

### Работает ✅
- Базовая структура приложения
- Dashboard с метриками
- Сделки (список, детали, доли)
- Реестры (список, экспорт)
- Выплаты (мониторинг)
- Контрагенты
- Документы
- Настройки

### Требует доработки 🔨
- Эмуляция ролей (UI)
- Инициатор сделки (UI)
- Stage-based ACL (logic)
- Field access tooltips (UI)
- Audit log viewer (UI)
- Extended mock data

### Готово к использованию 📦
- Типы данных
- Документация
- Базовые компоненты
- Экспорт данных
- Валидации полей

## Рекомендации

### Для дизайнеров
Используйте `EXTENDED_REQUIREMENTS.md` секции:
- "Примеры UI" для visual guidelines
- "Матрица прав" для понимания user flows
- "UI требования" для accessibility

### Для разработчиков
Начните с:
1. Изучения `types/index.ts` (новые интерфейсы)
2. Чтения `EXTENDED_REQUIREMENTS.md` (полный scope)
3. Имплементации `lib/access-control.ts` (core logic)
4. Обновления компонентов по фазам

### Для юристов
Проверьте:
- Модель audit log (раздел 7)
- Права доступа к документам (раздел 3)
- Идемпотентность операций (раздел 8.4)

---

**Статус:** Документация и типы готовы. UI компоненты требуют имплементации.
**Приоритет:** High - критично для production use
**Ответственный:** Frontend team
